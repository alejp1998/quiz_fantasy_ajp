<div class="right">
	<small> [Quiz by: 
		<%if(session.user){ %> 
			<a class="user" href="/users/<%= session.user.id %>" ><%= session.user.username%></a>
		<% }else{ %>
			Anonymous 
		<% } %>
	 ]</small>
</div>

<h2 class="show">Show Quiz <%=quiz.id%></h2>
    <% include ../upvotes/_arrowup.ejs %>
    <p class="question">Question: <%=quiz.question%></p>
    <p class="answer">Answer: <%=quiz.answer%></p> 

<h3>Quiz Tips:</h3>
 
<% if(session.user){ %>
	<% include ../tips/new %>
<% } %>

<% var tips = quiz.tips; %>
<% var quizId = quiz.id; %>
<% var quizAuthorId = quiz.authorId; %>


<% include ../tips/index %>

<a href="/goback" class="button">Go Back</a>	